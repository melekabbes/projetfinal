<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Mes Paiements</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: url('/images/cinema.gif') no-repeat center center fixed;
      background-size: cover;
      color: white;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
      padding: 2rem;
    }

    h1 {
      font-size: 3em;
      margin-bottom: 1.5rem;
      text-shadow: 2px 2px 6px rgba(0, 0, 0, 0.7);
    }

    table {
      border-collapse: collapse;
      width: 80%;
      background-color: rgba(0, 0, 0, 0.6);
      border-radius: 10px;
      overflow: hidden;
    }

    th, td {
      padding: 1rem;
      text-align: center;
      border-bottom: 1px solid #444;
    }

    th {
      background-color: #ffd700;
      color: #333;
      font-size: 1.2rem;
    }

    td {
      font-size: 1.1rem;
    }

    .retour {
      display: inline-block;
      margin-top: 2rem;
      padding: 0.6rem 1.2rem;
      background-color: red;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      font-weight: bold;
      box-shadow: 0 0 8px rgba(0, 0, 0, 0.4);
      transition: background-color 0.3s ease;
    }

    .retour:hover {
      background-color: darkred;
      text-decoration: underline;
    }
    .deconnexion {
      display: inline-block;
      margin-top: 2rem;
      padding: 0.8rem 1.5rem;
      background-color: #d9534f;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      font-weight: bold;
      border: none;
      cursor: pointer;
      font-size: 1rem;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
      transition: all 0.3s ease;
    }
    .deconnexion:hover {
      background-color: #c9302c;
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    }

    .deconnexion:active {
      transform: translateY(0);
      box-shadow: 0 2px 3px rgba(0, 0, 0, 0.3);
    }
  </style>
</head>
<body>
  <h1>Mes Paiements</h1>
  <% if (payments.length > 0) { %>
    <table>
      <thead>
        <tr>
          <th>Montant</th>
          <th>Méthode</th>
          <th>Statut</th>
          <th>Date</th>
          <th>Places réservées</th>
        </tr>
      </thead>
      <tbody>
        <% payments.forEach(function(payment) { %>
          <tr>
            <td><%= payment.montant %> €</td>
            <td><%= payment.methode %></td>
            <td><%= payment.statut %></td>
            <td><%= payment.date_paiement.toLocaleDateString('fr-FR') %> <%= payment.date_paiement.toLocaleTimeString('fr-FR') %></td>
            <td><%= payment.nb_places %></td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  <% } else { %>
    <p>Vous n'avez effectué aucun paiement pour le moment.</p>
  <% } %>
  <a href="/" class="deconnexion">Déconnexion</a>
</body>
</html>