<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Liste des Films - Queues</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      color: #f0f0f0;
      padding: 2rem 0;
    }
    .container {
      max-width: 1100px;
      background-color: rgba(0, 0, 0, 0.7);
      padding: 2rem 3rem;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.7);
    }
    h1 {
      font-weight: 700;
      color: #ffd700; 
      text-align: center;
      margin-bottom: 3rem;
      text-shadow: 1px 1px 5px rgba(0,0,0,0.8);
    }
    .film-card {
      background: #1a1a2e;
      border-radius: 15px;
      padding: 1.5rem;
      margin-bottom: 2.5rem;
      box-shadow: 0 6px 15px rgba(0,0,0,0.8);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .film-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 15px 30px rgba(255, 215, 0, 0.8);
    }
    .film-card h2 {
      color: #ffd700;
      font-size: 1.5rem;
      margin-bottom: 0.75rem;
      text-shadow: 1px 1px 3px rgba(0,0,0,0.7);
    }
    .film-card p {
      color: #ddd;
      font-size: 1rem;
      line-height: 1.4;
      margin-bottom: 1rem;
    }
    .film-image {
      max-width: 100%;
      height: auto;
      border-radius: 10px;
      box-shadow: 0 5px 15px rgba(255, 215, 0, 0.7);
      border: 2px solid #ffd700;
      display: block;
      margin: 0 auto;
    }
    .no-image {
      font-style: italic;
      color: #bbb;
      text-align: center;
      padding: 2rem 0;
      border: 2px dashed #555;
      border-radius: 10px;
      background-color: #222244;
    }
    @media (max-width: 768px) {
      .container {
        padding: 1rem 1.5rem;
      }
      .film-card h2 {
        font-size: 1.25rem;
      }
    }
.btn-danger:hover {
  background-color: red;
  color: white;
  box-shadow: 0 0 12px rgba(255, 165, 0, 0.9);
}
  </style>
</head>
<body>
  <div class="container">
    <h1>Liste des Films Disponibles</h1>
    <div class="row">
      <% films.forEach(film => { %>
        <div class="col-md-6 col-lg-4">
<div class="film-card">
  <h2><%= film.titre %></h2>
  <p><%= film.description %></p>
  <p>Prix : <%= film.prix %> dt</p>
  <p>Genre : <%= film.genre %></p>
  <% if (film.image_base64) { %>
    <img class="film-image" src="data:image/jpeg;base64,<%= film.image_base64 %>" alt="Affiche de <%= film.titre %>">
  <% } else { %>
    <div class="no-image">Aucune image disponible</div>
  <% } %>
<a href="/user-seance/<%= film.id %>" class="btn btn-warning mt-2 w-100">Consulter les séances</a>
</div>
        </div>
      <% }); %>
    </div>
    <div class="text-center mt-4">
  <a href="/" class="btn btn-danger btn-lg">Déconnexion</a>
</div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
